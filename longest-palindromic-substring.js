/**
 * @param {string} s
 * @return {string}
 */
const longestPalindrome = function(s) {
  let left = 0;
  let right = 0;
  const set = new Set();
  let result = '';

  while (left < s.length) {
    
  }
};
